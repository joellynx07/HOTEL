PORT=4000
NODE_ENV=development

DATABASE_URL=postgres://user:password@localhost:5432/spynx

AUTH_SECRET=replace-with-output-of-openssl-rand-base64-32

# The frontend's origin — used for CORS and for building links in emails.
APP_URL=http://localhost:5173

# Optional — verification emails just log to the console if unset.
RESEND_API_KEY=
