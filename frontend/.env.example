# Leave unset for local dev — Vite's dev-server proxy (vite.config.js)
# forwards /api/* to the backend on localhost:4000 automatically.
#
# In production, point this at your deployed backend's URL:
# VITE_API_URL=https://api.spynx.app
VITE_API_URL=
